# Database connection (for local development)
DATABASE_URL=postgresql://par_user:par_password@localhost:5432/par_db

# For Docker Compose, copy .env.docker.example to .env.docker and update values:
# POSTGRES_USER=par_user
# POSTGRES_PASSWORD=<change-me>
# POSTGRES_DB=par_db
# DATABASE_URL=postgresql://par_user:<change-me>@db:5432/par_db
