# PostgreSQL (localhost — all services run in the same LXC container)
DATABASE_URL=postgresql://par_user:par_password@localhost:5432/par_db

# Authentication (NextAuth.js)
NEXTAUTH_URL=https://par.cr.k12.de.us
NEXTAUTH_SECRET=change-me-to-a-random-secret

# Google OAuth (optional — leave empty to disable Google sign-in)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Default admin account (used by seed script on first run)
ADMIN_EMAIL=admin@cr.k12.de.us
ADMIN_PASSWORD=change-me-admin-password
